// Default Expo Metro config. Extend here if custom resolver/transformer settings are needed.
const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

module.exports = config;
